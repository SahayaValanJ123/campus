{
  "name": "campus-sphere",
  "version": "1.0.0",
  "description": "CampusSphere - Multi-College Student Platform App",
  "main": "index.html",
  "scripts": {
    "build": "node build.js",
    "build:android": "npm run build && npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  }
}
