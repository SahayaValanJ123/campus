{
  "name": "vv-campuspulse",
  "version": "1.0.0",
  "description": "VV CampusPulse - Exclusive All-in-One College Platform App",
  "main": "index.html",
  "scripts": {
    "build:android": "npx cap copy android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  }
}
